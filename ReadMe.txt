The following steps needs to be performed in order to run the app

Use
pip install -r requirements.txt
to install django and django restframework

Use 
npm i
to install all dependencies

Use 
npm run dev
to compile the react code

To start django server use
python manage.py runserver

